before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y texinfo
script:
  - make info
  - make init
  - make fetchtoolchain
  - make toolchain
  - make env >environment
  - cat ./environment
  - make linux  
